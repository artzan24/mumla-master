<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/channel_row_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingLeft="12dp"
    android:paddingRight="12dp"
    android:background="?attr/selectableItemBackground">

    <!-- Ikon Kiri (Ukurannya dibuat tetap 44dp x 44dp agar seragam dan rapi) -->
    <ImageView
        android:id="@+id/channel_row_expand"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="center_vertical"
        android:background="@drawable/outline_circle_talking_off"
        android:tint="?android:attr/textColorSecondary"
        android:padding="10dp" />

    <!-- Kontainer Teks Nama & Status Channel -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:layout_gravity="center_vertical">

        <TextView
            android:id="@+id/channel_row_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical|left"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorPrimary"
            android:singleLine="true"
            android:text="Channel Name"/>

        <TextView
            android:id="@+id/channel_row_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical|left"
            android:textSize="13sp"
            android:textColor="?android:attr/textColorSecondary"
            android:layout_marginTop="2dp"
            android:singleLine="true"
            android:text="Users connected"/>
    </LinearLayout>

    <!-- Tombol Join (Telepon/Move) di Kanan -->
    <ImageView
        android:id="@+id/channel_row_join"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="center_vertical"
        app:srcCompat="@drawable/ic_action_move"
        android:tint="#4CAF50"
        android:padding="6dp"
        android:background="?attr/selectableItemBackgroundBorderless"/>

    <!-- Tombol Opsi Titik Tiga -->
    <ImageView
        android:id="@+id/channel_row_more"
        android:layout_width="32dp"
        android:layout_height="36dp"
        android:layout_gravity="center_vertical"
        app:srcCompat="@drawable/ic_more_vert_black_24dp"
        android:paddingLeft="8dp"
        android:tint="?android:attr/textColorSecondary"
        android:background="?attr/selectableItemBackgroundBorderless"/>
</LinearLayout>